import 'package:flutter/material.dart';

class WidgetPaymentCard extends StatelessWidget {
  const WidgetPaymentCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
