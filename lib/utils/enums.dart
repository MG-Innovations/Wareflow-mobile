enum EnumPaymentStatus {
  // Orders
  paymentPending,
  paymentPartial,
  paymentCompleted,

  // Product
  productOutOfStock,
  productLowStock,
}

enum TextInputType { text, number, email, phone, password, multiline, datetime }
